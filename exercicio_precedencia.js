//FAZER A MÃO

//Exercício 01 (foi True)
let a = 5,
  b = 10,
  c = 15,
  d = 20;
var resultado = !(a + b * 2 > c) && (d / 2 == 10 || c - b <= a + 1);

//Exercício 02 (foi False)
let x = 8,
  y = 3,
  z = 12,
  w = 4;
var resultado = !(x - y * 2 <= z / w) || (z % x >= y + w && !(z - x < y * w));
